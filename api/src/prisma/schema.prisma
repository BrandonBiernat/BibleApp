generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model translations {
  id           String @id @default(uuid()) @db.Uuid
  abbreviation String
  name         String
  method       String
  language     String
  year         Int
}
